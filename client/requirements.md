## Packages
framer-motion | For smooth, minimalist page transitions and fade-ins
lucide-react | For clean, minimalist icons

## Notes
The application requires a minimalist approach as requested. 
The backend provides a single endpoint `/api/message` which we will fetch.
